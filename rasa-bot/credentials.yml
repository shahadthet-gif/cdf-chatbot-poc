rest:
  # No credentials required. The custom website widget POSTs to
  # http://localhost:5005/webhooks/rest/webhook
  # CORS for the frontend's local dev origin is set via the --cors flag
  # when starting `rasa run` (see rasa-bot/README.md).
